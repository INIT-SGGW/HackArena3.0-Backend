[package]
name = "boink"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = { workspace = true }

boink-sys = { path = "../boink-sys" }

libloading = { version = "0.9", optional = true }
thiserror = "2.0.17"

[features]
default = []
legacy-native-lib = ["libloading"]
