# Application environment: development | preprod | production
APP_ENV=development

# gRPC server listen address
LISTEN_ADDR=0.0.0.0:50051

# Logging
RUST_LOG=warn,boink=info,tonic_web=info,game_server=info,game_engine=info,game_server::config=debug

# Allowed CORS origins
CORS_ALLOWED_ORIGINS=

# Optional: additional headers to expose (comma-separated)
# CORS_EXPOSE_HEADERS=X-Trace-Id

# Game-token JWT settings used by `ha3-backend-official`
GAME_JWKS_URL=https://platform.dev.hackarena.pl/.well-known/jwks.json
GAME_JWT_OFFICIAL_AUDIENCE=ha3-official
GAME_JWT_OFFICIAL_ISSUERS=ha3-auth

# Official backend database URL (Postgres)
OFFICIAL_DATABASE_URL=postgres://user:pass@localhost:5432/ha3_official_dev

# Optional: max size of the official DB connection pool
# OFFICIAL_DB_MAX_CONNECTIONS=8

# Build-service integration (backend -> build-service gRPC)
BUILD_SERVICE_GRPC_ENDPOINT=http://127.0.0.1:50053
BUILD_SERVICE_SUBMIT_TIMEOUT_MS=5000
BUILD_SERVICE_GET_TIMEOUT_MS=5000
BUILD_SERVICE_LIST_TIMEOUT_MS=5000
BUILD_SERVICE_CANCEL_TIMEOUT_MS=5000

# Build upload staging (backend local disk)
BUILD_UPLOADS_ROOT=uploads/build
BUILD_UPLOAD_MAX_SIZE_BYTES=33554432

# HPS integration (backend -> HackArena Platform Service gRPC)
HPS_ENDPOINT=http://127.0.0.1:50052
HPS_GET_TIMEOUT_MS=5000
HPS_CACHE_TTL_MS=3600000
HPS_EDITION=3
